# Security Credentials
.env
credentials.json

# Python Cache (optional, but good practice to keep things clean)
__pycache__/
*.pyc

# Downloaded PDFs (so you don't upload weekly reports to GitHub)
*.pdf